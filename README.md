#This is ReadMe for SimpliLearn Project
#Project uses simple few unix shell scripts as source code
# Jenkinspipeline is used for automated build along with creation of image and pushing to docker hub
